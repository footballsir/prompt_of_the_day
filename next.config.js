/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable Next.js development indicators
  devIndicators: {
    position: 'bottom-right', // This moves it but we can't fully disable it
  },
}

module.exports = nextConfig
